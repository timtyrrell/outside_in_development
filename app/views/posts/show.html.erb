<%= flash[:notice] if flash[:notice] %>

<%= @post.title %>
<%= @post.body %>

<%= form_for [@post, @post.comments.build] do |f| %>
  <%= f.label :retort %>
  <%= f.text_field :retort %>
  <%= f.button "Add Comment" %>
<% end %>
